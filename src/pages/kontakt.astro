---
import { CustomProvider } from "rsuite";
import Layout from "../layouts/Layout.astro";
---

<Layout>
  <CustomProvider>
    <form
      action="https://formspree.io/f/xpwdggnn"
      method="POST"
      enctype="multipart/form-data"
      class="max-w-md mx-auto bg-white p-6 rounded-lg shadow-md space-y-5"
    >
      <h2 class="text-2xl font-bold mb-4 text-gray-800">
        Dele kunnskap eller stille spørsmål?
      </h2>
      <p class="pb-4">
        Vi ønsker å vere et nyttig supplement til den amerikanske klubben, så
        del gjerne erfaringer og kunnskap med oss.
      </p>

      <div>
        <label class="block text-gray-700 font-medium mb-1" for="name">
          Navn
        </label>
        <input
          type="text"
          name="name"
          id="name"
          required
          class="w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500"
          placeholder="Ditt navn"
        />
      </div>

      <div>
        <label class="block text-gray-700 font-medium mb-1" for="email">
          E-post
        </label>
        <input
          type="email"
          name="email"
          id="email"
          required
          class="w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500"
          placeholder="din@email.no"
        />
      </div>
      <label>
        Last opp bilde
        <input type="file" name="upload" />
      </label>
      <div>
        <label class="block text-gray-700 font-medium mb-1" for="message">
          Melding
        </label>
        <textarea
          name="message"
          id="message"
          rows="4"
          required
          class="w-full p-2.5 border border-gray-300 rounded resize-y focus:outline-none focus:ring-2 focus:ring-blue-500"
          placeholder="Skriv en melding, f.eks. informasjon om bilen, historie, eller spørsmål"
        ></textarea>
      </div>

      <button
        type="submit"
        class="w-full bg-blue-600 text-white font-semibold py-2 px-4 rounded hover:bg-blue-700 transition-colors"
      >
        Send skjema
      </button>
    </form>
  </CustomProvider>
</Layout>
